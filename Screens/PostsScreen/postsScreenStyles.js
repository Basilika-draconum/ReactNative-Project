import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    flex: 1,
  },
  user: {
    fontFamily: "Roboto-Bold",
    fontSize: 13,
    paddingTop: 50,
    alignSelf: "center",
  },
});
